<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-devour="{{devour}}">
		{{#if devour}}EAT AGAIN!{{else}}DEVOUR!{{/if}}
	</button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>